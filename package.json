{
  "name": "stems-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A Swedish-language desktop-style audio stem separation interface.",
  "scripts": {
    "check": "node --check src/main.js && python3 -m py_compile server.py",
    "start": "python3 server.py"
  }
}
